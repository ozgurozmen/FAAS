/**********************************************************************
 *                                                                    *
 * Created by Adam Brockett and Edited by Muslum Ozgur Ozmen          *
 *                                                                    *                                                       
 *                                                                    *
 **********************************************************************/

#include <stdlib.h>
#include <stdio.h>
#include <string.h>
#include <gmp.h>
#include <time.h>
#include <math.h>


#include "blake2.h"
//#include "blake2-kat.h"


#define MODULUS_SIZE 3072                   /* This is the number of bits we want in the modulus */
#define BLOCK_SIZE (MODULUS_SIZE/8)         /* This is the size of a block that gets en/decrypted at once */
#define BUFFER_SIZE ((MODULUS_SIZE/8) / 2)  /* This is the number of bytes in n and p */

typedef struct {
    mpz_t n; /* Modulus */
    mpz_t e; /* Public Exponent */
} public_key;

typedef struct {
    mpz_t n; /* Modulus */
    mpz_t e; /* Public Exponent */
    mpz_t d; /* Private Exponent */
    mpz_t p; /* Starting prime p */
    mpz_t q; /* Starting prime q */
} private_key;

void print_hex(char* arr, int len)
{
    int i;
    for(i = 0; i < len; i++)
        printf("%02x", (unsigned char) arr[i]);
}

/* NOTE: Assumes mpz_t's are initted in ku and kp */
void generate_keys(private_key* ku, public_key* kp)
{
    char buf[BUFFER_SIZE];
    int i;
    mpz_t phi; mpz_init(phi);
    mpz_t tmp1; mpz_init(tmp1);
    mpz_t tmp2; mpz_init(tmp2);

    srand(time(NULL));

    /* Insetead of selecting e st. gcd(phi, e) = 1; 1 < e < phi, lets choose e
     * first then pick p,q st. gcd(e, p-1) = gcd(e, q-1) = 1 */
    // We'll set e globally.  I've seen suggestions to use primes like 3, 17 or
    // 65537, as they make coming calculations faster.  Lets use 3.
    mpz_set_ui(ku->e, 65537);

    /* Select p and q */
    /* Start with p */
    // Set the bits of tmp randomly
    for(i = 0; i < BUFFER_SIZE; i++)
        buf[i] = rand() % 0xFF;
    // Set the top two bits to 1 to ensure int(tmp) is relatively large
    buf[0] |= 0xC0;
    // Set the bottom bit to 1 to ensure int(tmp) is odd (better for finding primes)
    buf[BUFFER_SIZE - 1] |= 0x01;
    // Interpret this char buffer as an int
    mpz_import(tmp1, BUFFER_SIZE, 1, sizeof(buf[0]), 0, 0, buf);
    // Pick the next prime starting from that random number
    mpz_nextprime(ku->p, tmp1);
    /* Make sure this is a good choice*/
    mpz_mod(tmp2, ku->p, ku->e);        /* If p mod e == 1, gcd(phi, e) != 1 */
    while(!mpz_cmp_ui(tmp2, 1))
    {
        mpz_nextprime(ku->p, ku->p);    /* so choose the next prime */
        mpz_mod(tmp2, ku->p, ku->e);
    }

    /* Now select q */
    do {
        for(i = 0; i < BUFFER_SIZE; i++)
            buf[i] = rand() % 0xFF;
        // Set the top two bits to 1 to ensure int(tmp) is relatively large
        buf[0] |= 0xC0;
        // Set the bottom bit to 1 to ensure int(tmp) is odd
        buf[BUFFER_SIZE - 1] |= 0x01;
        // Interpret this char buffer as an int
        mpz_import(tmp1, (BUFFER_SIZE), 1, sizeof(buf[0]), 0, 0, buf);
        // Pick the next prime starting from that random number
        mpz_nextprime(ku->q, tmp1);
        mpz_mod(tmp2, ku->q, ku->e);
        while(!mpz_cmp_ui(tmp2, 1))
        {
            mpz_nextprime(ku->q, ku->q);
            mpz_mod(tmp2, ku->q, ku->e);
        }
    } while(mpz_cmp(ku->p, ku->q) == 0); /* If we have identical primes (unlikely), try again */

    /* Calculate n = p x q */
    mpz_mul(ku->n, ku->p, ku->q);

    /* Compute phi(n) = (p-1)(q-1) */
    mpz_sub_ui(tmp1, ku->p, 1);
    mpz_sub_ui(tmp2, ku->q, 1);
    mpz_mul(phi, tmp1, tmp2);

    /* Calculate d (multiplicative inverse of e mod phi) */
    if(mpz_invert(ku->d, ku->e, phi) == 0)
    {
        mpz_gcd(tmp1, ku->e, phi);
        printf("gcd(e, phi) = [%s]\n", mpz_get_str(NULL, 16, tmp1));
        printf("Invert failed\n");
    }

    /* Set public key */
    mpz_set(kp->e, ku->e);
    mpz_set(kp->n, ku->n);

    return;
}

void block_encrypt(mpz_t C, mpz_t M, public_key kp)
{
    /* C = M^e mod n */
    mpz_powm(C, M, kp.e, kp.n);
    return;
}

int encrypt(char cipher[], char message[], int length, public_key kp)
{
    /* Its probably overkill, but I implemented PKCS#1v1.5 paging
     * Encoded message block is of the form:
     * EMB = 00 || 02 || PS || 00 || D
     * Where || is concatenation, D is the message, and PS is a string of
     * (block_size-|D|-3) non-zero, randomly generated bytes
     * |D| must be less than block_size - 11, which means we have at least 8
     * bytes of PS
     */
    int block_count = 0;
    int prog = length;
    char mess_block[BLOCK_SIZE];
    mpz_t m; mpz_init(m);
    mpz_t c; mpz_init(c);

    while(prog > 0)
    {
        int i = 0;
        int d_len = (prog >= (BLOCK_SIZE - 11)) ? BLOCK_SIZE - 11 : prog;

        /* Construct the header */
        mess_block[i++] = 0x00;
        mess_block[i++] = 0x02;
        while(i < (BLOCK_SIZE - d_len - 1))
            mess_block[i++] = (rand() % (0xFF - 1)) + 1;
        mess_block[i++] = 0x00;

        /* Copy in the message */
        memcpy(mess_block + i, message + (length - prog), d_len);

        // Convert bytestream to integer
        mpz_import(m, BLOCK_SIZE, 1, sizeof(mess_block[0]), 0, 0, mess_block);
        // Perform encryption on that block
        block_encrypt(c, m, kp);

        // Calculate cipher write offset to take into account that we want to
        // pad with zeros in the front if the number we get back has fewer bits
        // than BLOCK_SIZE
        int off = block_count * BLOCK_SIZE;         // Base offset to start of this block
        off += (BLOCK_SIZE - (mpz_sizeinbase(c, 2) + 8 - 1)/8); // See manual for mpz_export

        // Pull out bytestream of ciphertext
        mpz_export(cipher + off, NULL, 1, sizeof(char), 0, 0, c);

        block_count++;
        prog -= d_len;
    }
    return block_count * BLOCK_SIZE;
}

void block_decrypt(mpz_t M, mpz_t C, private_key ku)
{
    mpz_powm(M, C, ku.d, ku.n);
    return;
}

int decrypt(char* message, char* cipher, int length, private_key ku)
{
    int msg_idx = 0;
    char buf[BLOCK_SIZE];
    *(long long*)buf = 0ll;
    mpz_t c; mpz_init(c);
    mpz_t m; mpz_init(m);

    int i;
    for(i = 0; i < (length / BLOCK_SIZE); i++)
    {
        // Pull block into mpz_t
        mpz_import(c, BLOCK_SIZE, 1, sizeof(char), 0, 0, cipher + i * BLOCK_SIZE);
        // Decrypt block
        block_decrypt(m, c, ku);

        // Calculate message write offset to take into account that we want to
        // pad with zeros in the front if the number we get back has fewer bits
        // than BLOCK_SIZE
        int off = (BLOCK_SIZE - (mpz_sizeinbase(m, 2) + 8 - 1)/8); // See manual for mpz_export
        // Convert back to bitstream
        mpz_export(buf + off, NULL, 1, sizeof(char), 0, 0, m);

        // Now we just need to lop off top padding before memcpy-ing to message
        // We know the first 2 bytes are 0x00 and 0x02, so manually skip those
        // After that, increment forward till we see a zero byte
        int j;
        for(j = 2; ((buf[j] != 0) && (j < BLOCK_SIZE)); j++);
        j++;        // Skip the 00 byte

        /* Copy over the message part of the plaintext to the message return var */
        memcpy(message + msg_idx, buf + j, BLOCK_SIZE - j);

        msg_idx += BLOCK_SIZE - j;
    }
    return msg_idx;
}

int main()
{
    mpz_t M;  mpz_init(M);
    mpz_t C;  mpz_init(C);
    mpz_t DC;  mpz_init(DC);
    private_key ku;
    public_key kp;
    mpz_t rBig, R, sBig, alpha, vPrime;

    mpz_init(sBig);
    mpz_init(alpha);
    mpz_init(vPrime);

    mpz_set_ui(vPrime,1);
    mpz_set_ui(alpha,1);

    // Initialize public key
    mpz_init(kp.n);
    mpz_init(kp.e);
    // Initialize private key
    mpz_init(ku.n);
    mpz_init(ku.e);
    mpz_init(ku.d);
    mpz_init(ku.p);
    mpz_init(ku.q);

    //mpz_set_ui(M, 999999999888888887777777666666555554444333221);
	uint8_t message[32] = {0};

    generate_keys(&ku, &kp);
//    printf("---------------Private Key-----------------\n");
//    printf("kp.n is [%s]\n", mpz_get_str(NULL, 16, kp.n));
//    printf("kp.e is [%s]\n", mpz_get_str(NULL, 16, kp.e));
//    printf("---------------Public Key------------------\n");
//    printf("ku.n is [%s]\n", mpz_get_str(NULL, 16, ku.n));
//    printf("ku.e is [%s]\n", mpz_get_str(NULL, 16, ku.e));
//    printf("ku.d is [%s]\n", mpz_get_str(NULL, 16, ku.d));
//    printf("ku.p is [%s]\n", mpz_get_str(NULL, 16, ku.p));
//    printf("ku.q is [%s]\n", mpz_get_str(NULL, 16, ku.q));


    //mpz_import(M, (6*BLOCK_SIZE), 1, sizeof(buf[0]), 0, 0, buf);
    int ii = 1;
    double timeSign, timeVer, timeBlake;
    timeSign = 0.0;
    timeVer = 0.0;
    timeBlake = 0.0;
    clock_t start, start2;
	clock_t end, end2;
	clock_t hashS, hashE;
	
	unsigned char z[32] = {0x54, 0xa2, 0xf8, 0x03, 0x1d, 0x18, 0xac, 0x77, 0xd2, 0x53, 0x92, 0xf2, 0x80, 0xb4, 0xb1, 0x2f, 0xac, 0xf1, 0x29, 0x3f, 0x3a, 0xe6, 0x77, 0x7d, 0x74, 0x15, 0x67, 0x91, 0x99, 0x53, 0x69, 0xc5};
	mpz_init(rBig);
	mpz_init(R);

	uint8_t hash = 0;
	uint8_t hash2[2] = {0};
//	uint8_t hash2 = 0;
//	hash = &hash2;
	unsigned char tempor[32];
	unsigned char tempor2[16];
	mpz_t sigma[256];
	mpz_t messages[256];
	mpz_t Beta[1024];
	mpz_t temp;
	mpz_init(temp);
	int t = 16;
	int k = 1024;
	
	for (unsigned i = 0; i < k; ++i) {
		if (i>=768){
			hash2[1] = 3;
			hash2[0] = i-768;
		}
		if ((i>=512) & (i <768)){
			hash2[1] = 2;
			hash2[0] = i-512;
		}
		if ((i>=256) & (i<512)){
			hash2[1] = 1;
			hash2[0] = i-256;
		}
		else
			hash2[0] = i;
		blake2b(tempor2, hash2, z, 16, 2, 32);
		mpz_import(temp,sizeof(tempor2),1,sizeof(tempor2[0]),0,0, tempor2);
		mpz_init(Beta[i]);
		block_decrypt(Beta[i],temp,ku);
	}
	
	//printf("here");
	for (unsigned i = 0; i < 256; ++i) {
		hash = hash +1;
		blake2b(tempor, &hash, NULL, 32, 1, 0);
		mpz_init(messages[i]);
		mpz_import(messages[i],sizeof(tempor),1,sizeof(tempor[0]),0,0, tempor);
		mpz_init(sigma[i]);
		block_decrypt(sigma[i],messages[i],ku);
		//printf("Sigma \n");
		//gmp_printf ("%Zd\n", sigma[i]);
		//mpz_invert(sigma[i], sigma[i], kp.n);
		//block_encrypt(temp, sigma[i], kp);
//		printf("Temp2 \n");
//		gmp_printf ("%Zd\n", messages[i]);
	}

	unsigned char hashedM[32];
	unsigned char signatureHash[32];
	unsigned char signatureHashVer[32];
	unsigned char r[32];
	unsigned char s[16];
	unsigned char randNum[16];
	unsigned char randNum2[32];
//	unsigned char concat[48] = {0};
    unsigned char *concat = malloc(48);
	unsigned char compare[BLOCK_SIZE];

	int place;
	mpz_t alphaEnc;
	mpz_t u, sigmaU;
	mpz_init(alphaEnc);
	mpz_init(u);
	mpz_init(sigmaU);

	//=====================SIGN===========================
//	for (ii = 0; ii < 10000; ++ii) {
		mpz_set_ui(alpha,1);
		mpz_set_ui(u,1);
		mpz_set_ui(sigmaU,1);
		start = clock();
		
		for (unsigned i = 0; i < 32; ++i) {
			randNum2[i] = rand()*255/RAND_MAX;
		}
		
		for (unsigned i = 0; i < t; ++i) {
			hash2[0] = randNum2[2*i];
			hash2[1] = floor(randNum2[2*i+1]/64);
			place = hash2[0] + hash2[1]*256;
			blake2b(s, hash2, z, 16, 2, 32);
			mpz_import(sBig,sizeof(s),1,sizeof(s[0]),0,0, s);
			mpz_mul(u,sBig,u);
			mpz_mul(sigmaU, Beta[place], sigmaU);
			mpz_mod(sigmaU, sigmaU, kp.n);
		}
		mpz_mod(u, u, kp.n);
				
		
		for (unsigned i = 0; i < 16; ++i) {
			randNum[i] = rand()*255/RAND_MAX;
		}
		memcpy(concat, &message, 32);
		memcpy(concat + 32, &randNum, 16);
		
		blake2b(hashedM, concat, NULL, 32,32,0);


		for (unsigned i = 0; i < 32; ++i) {
			mpz_mul(alpha,sigma[hashedM[i]],alpha);
			mpz_mod(alpha,alpha,kp.n);
		}
		mpz_mul(alpha, sigmaU, alpha);
		mpz_mod(alpha, alpha, kp.n);

		end = clock();
		timeSign = timeSign + (double)(end-start);


		//=====================VERIFY===========================
		mpz_set_ui(vPrime,1);
		start2 = clock();
		memcpy(concat, &message, 32);
		memcpy(concat + 32, &randNum, 16);
		blake2b(hashedM, concat, NULL, 32,32,0);

		for (unsigned i = 0; i < 32; ++i) {
			hash = hashedM[i]+1;
			blake2b(tempor, &hash, NULL, 32, 1, 0);
			mpz_import(temp,sizeof(tempor),1,sizeof(tempor[0]),0,0, tempor);
			mpz_mul(vPrime, temp, vPrime);
		}
		mpz_mul(vPrime, u, vPrime);
		mpz_mod(vPrime, vPrime, kp.n);
		block_encrypt(alphaEnc,alpha,kp);

		end2 = clock();

		timeVer = timeVer + (double)(end2-start2);
//	}


	if (mpz_cmp(vPrime,alphaEnc) == 0)
		printf("SIGNATURE IS VERIFIED\n");
	else
		printf("SIGNATURE IS NOT VERIFIED\n");


	printf("Parameters:\n");
	printf("|N| = %d\n", MODULUS_SIZE);
	printf("e = %d\n", 65537);
	printf("%fus per sign\n", ((double) (timeSign * 1000)) / CLOCKS_PER_SEC / ii * 1000);
	printf("%fus per verification\n", ((double) (timeVer * 1000)) / CLOCKS_PER_SEC / ii * 1000);
	printf("%fus end-to-end delay\n", ((double) ((timeSign+timeVer) * 1000)) / CLOCKS_PER_SEC / ii * 1000);	

    return 0;
}
